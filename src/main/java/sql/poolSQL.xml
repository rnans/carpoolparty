<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sql.poolSQL">
	<select id="getUserInfo" parameterType="String" resultType="su.member.model.MemberDTO">
		select * from userinfo where id=#{id}
	</select>
	<insert id="poolMemberShortAdd" parameterType="su.pool.model.PoolDTO">
		insert into reservationlist
		(idx,userid,aim,startspot,endspot,route,starttime,mannum,pay,smoking,pluscontent,gender,writedate,status,pooltype,termtype,startdate,enddate) 
		values(
		reservationlist_idx.nextval,
		#{userid},
		#{aim},
		#{startspot},
		#{endspot},
		#{route},
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		#{mannum},
		#{pay},
		#{smoking},
		#{pluscontent},
		#{gender},
		sysdate,
		'모집중',
		#{pooltype},
		#{termtype},
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi')
		)
	</insert>
 	<insert id="poolMemberLongAdd" parameterType="su.pool.model.PoolDTO">
		insert into reservationlist
		(idx,userid,aim,startspot,endspot,route,starttime,mannum,pay,smoking,pluscontent,gender,writedate,status,pooltype,termtype,startdate,enddate,days) 
		values(
		reservationlist_idx.nextval,
		#{userid},
		#{aim},
		#{startspot},
		#{endspot},
		#{route},
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		#{mannum},
		#{pay},
		#{smoking},
		#{pluscontent},
		#{gender},
		sysdate,
		'모집중',
		#{pooltype},
		#{termtype},
		TO_date(#{startdate},'yyyy-mm-dd'),
		TO_date(#{enddate},'yyyy-mm-dd'),
		#{days}
		)
	</insert>
	<insert id="poolMasterShortAdd" parameterType="su.pool.model.PoolDTO">
		insert into reservationlist
		(idx,userid,aim,startspot,endspot,route,starttime,mannum,pay,smoking,pluscontent,gender,writedate,status,pooltype,termtype,startdate,enddate,poolname,caridx) 
		values(
		reservationlist_idx.nextval,
		#{userid},
		#{aim},
		#{startspot},
		#{endspot},
		#{route},
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		#{mannum},
		#{pay},
		#{smoking},
		#{pluscontent},
		#{gender},
		sysdate,
		'모집중',
		#{pooltype},
		#{termtype},
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		#{poolname},
		#{caridx}
		)
	</insert>
 	<insert id="poolMasterLongAdd" parameterType="su.pool.model.PoolDTO">
		insert into reservationlist
		(idx,userid,aim,startspot,endspot,route,starttime,mannum,pay,smoking,pluscontent,gender,writedate,status,pooltype,termtype,startdate,enddate,days,poolname,caridx) 
		values(
		reservationlist_idx.nextval,
		#{userid},
		#{aim},
		#{startspot},
		#{endspot},
		#{route},
		TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		#{mannum},
		#{pay},
		#{smoking},
		#{pluscontent},
		#{gender},
		sysdate,
		'모집중',
		#{pooltype},
		#{termtype},
		TO_date(#{startdate},'yyyy-mm-dd'),
		TO_date(#{enddate},'yyyy-mm-dd'),
		#{days},
		#{poolname},
		#{caridx}
		)
	</insert>
	
	
	<select id="totalCnt" resultType="int">
		select count(*) from reservationlist
	</select>
	
	<select id="shortTotalCnt" resultType="int">
		select count(*) from reservationlist where type='단기'
	</select>
	
	
	<select id="longTotalCnt" resultType="int">
		select count(*) from reservationlist where type='정기'
	</select>
	
	<select id="viewAllList" parameterType="Map" resultType="su.pool.model.PoolDTO">
		select 
			idx,
			userid,
			aim,
			startspot,
			endspot,
			route,
			startcoordi,
			endcoordi,
			routecoordi,
			to_char(starttime,'YYYY-MM-DD HH24:Mi') starttime,
			mannum,
			gender,
			pay,
			smoking,
			pluscontent,
			to_char(writedate,'yyyy-mm-dd') writedate,
			status,
			termtype,
			to_char(startdate,'yyyy-mm-dd') startdate,
			to_char(enddate,'yyyy-mm-dd') enddate,
			days,
			location 
		from
	   (select rownum as rnum,a.* from
	   (select * from reservationlist order by idx desc)a)b
	   where rnum<![CDATA[>=]]>#{startnum} and rnum<![CDATA[<=]]>#{endnum}
	</select>
	
	<select id="viewShortList" parameterType="Map" resultType="su.pool.model.PoolDTO">
		select 
			idx,
			userid,
			aim,
			startspot,
			endspot,
			route,
			startcoordi,
			endcoordi,
			routecoordi,
			to_char(starttime,'YYYY-MM-DD HH24:Mi') starttime,
			mannum,
			gender,
			pay,
			smoking,
			pluscontent,
			to_char(writedate,'yyyy-mm-dd') writedate,
			status,
			termtype,
			to_char(startdate,'yyyy-mm-dd') startdate,
			to_char(enddate,'yyyy-mm-dd') enddate,
			days,
			location 
		from
	   (select rownum as rnum,a.* from
	   (select * from reservationlist where type='단기' order by idx desc)a)b
	   where rnum<![CDATA[>=]]>#{startnum} and rnum<![CDATA[<=]]>#{endnum}
	</select>
	
	<select id="viewLongList" parameterType="Map" resultType="su.pool.model.PoolDTO">
		select 
			idx,
			userid,
			aim,
			startspot,
			endspot,
			route,
			startcoordi,
			endcoordi,
			routecoordi,
			to_char(starttime,'YYYY-MM-DD HH24:Mi') starttime,
			mannum,
			gender,
			pay,
			smoking,
			pluscontent,
			to_char(writedate,'yyyy-mm-dd') writedate,
			status,
			termtype,
			to_char(startdate,'yyyy-mm-dd') startdate,
			to_char(enddate,'yyyy-mm-dd') enddate,
			days,
			location 
		from
	   (select rownum as rnum,a.* from
	   (select * from reservationlist where type='정기' order by idx desc)a)b
	   where rnum<![CDATA[>=]]>#{startnum} and rnum<![CDATA[<=]]>#{endnum}
	</select>
	
	<select id="viewEachContent" parameterType="int" resultType="su.pool.model.PoolDTO">
		select 
			idx,
			userid,
			aim,
			startspot,
			endspot,
			route,
			startcoordi,
			endcoordi,
			routecoordi,
			to_char(starttime,'YYYY-MM-DD HH24:MI') starttime,
			mannum,
			gender,
			pay,
			smoking,
			pluscontent,
			to_char(writedate,'yyyy-mm-dd') writedate,
			status,
			termtype,
			to_char(startdate,'yyyy-mm-dd') startdate,
			to_char(enddate,'yyyy-mm-dd') enddate,
			days,
			location 
		from reservationlist where idx=#{idx}
	</select>
	
	<update id="poolMemberShortEdit" parameterType="su.pool.model.PoolDTO">
		update reservationlist set 
		userid=#{userid},
		aim=#{aim},
		startspot=#{startspot},
		endspot=#{endspot},
		route=#{route},
		starttime=TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		mannum=#{mannum},
		gender=#{gender},
		pay=#{pay},
		smoking=#{smoking},
		pluscontent=#{pluscontent},
		termtype=#{termtype},
		startdate=TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		enddate=TO_date(#{starttime},'yyyy-mm-dd hh24:mi') 
		where idx=#{idx}
	</update>
	
	<update id="poolMemberLongEdit" parameterType="su.pool.model.PoolDTO">
		update reservationlist set 
		userid=#{userid},
		aim=#{aim},
		startspot=#{startspot},
		endspot=#{endspot},
		route=#{route},
		starttime=TO_date(#{starttime},'yyyy-mm-dd hh24:mi'),
		mannum=#{mannum},
		gender=#{gender},
		pay=#{pay},
		smoking=#{smoking},
		pluscontent=#{pluscontent},
		termtype=#{termtype},
		startdate=TO_date(#{startdate},'yyyy-mm-dd'),
		enddate=TO_date(#{enddate},'yyyy-mm-dd'),
		days=#{days} 
		where idx=#{idx}
	</update>
	
	<delete id="poolDel" parameterType="int">
		delete from reservationlist where idx=#{idx}
	</delete>
</mapper>
