<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="sql.carbookinfo">
    	<insert id="CarBookInfo" parameterType="su.carBookInfo.model.CarBookInfoDTO">
    		insert into carbookinfo values(carbookinfo_idx.nextval,
    		#{id},#{carkind1},#{carkind2},#{carkind3},#{carnum},#{oilcost})
    	</insert>
    </mapper>