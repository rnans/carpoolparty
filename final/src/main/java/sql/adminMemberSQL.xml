<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sql.adminMember">
	<select id="memberInfo" resultType="su.adminMember.model.AdminMemberDTO">
		select * from userinfo order by idx desc
	</select>
	
	<select id="memberList" resultType="su.adminMember.model.AdminMemberDTO">
		select * from userinfo where idx=#{idx}
	</select>
	
	<delete id="memberDel" parameterType="su.adminMember.model.AdminMemberDTO">
		delete from userinfo where idx=#{idx}
	</delete>

	<update id="adminAdd" parameterType="su.adminMember.model.AdminMemberDTO">
		update userinfo set grade=#{grade} where idx=#{idx}
	</update>
</mapper>